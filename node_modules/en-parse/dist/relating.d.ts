import { NodeInterface } from "./index";
export default function (nodes: NodeInterface[], recursionLimit: number): NodeInterface[];
export interface MatchResult {
    direction: string;
    label: string;
}
export declare const findBy: {
    type: (type: string, nodes: NodeInterface[]) => boolean;
    label: (label: string, nodes: NodeInterface[]) => boolean;
};
