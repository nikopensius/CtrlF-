export declare const tree: (tags: string[], tokens: string[]) => NodeInterface[];
export declare const parse: (tags: string[], tokens: string[]) => ResultNode[];
export interface NodeInterface {
    left: Array<NodeInterface>;
    right: Array<NodeInterface>;
    tokens: Array<string>;
    tags: Array<string>;
    index: Array<number>;
    type: string;
    label: string;
}
export interface ResultNode {
    label: string;
    type: string;
    parent: number;
}
export declare function toArray(jsonTree: NodeInterface, parent?: number, arr?: ResultNode[]): ResultNode[];
export default parse;
